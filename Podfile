platform :ios, '9.0'
use_frameworks!

target 'DailyQuote' do
  # networking
  pod 'Alamofire', '~> 3.4'
  # json parsing
  pod 'ObjectMapper', '~> 1.3'
  pod 'AlamofireObjectMapper', '~> 3.0'
  # image loader
  pod 'Kingfisher', '~> 2.4'
  

  target 'DailyQuoteTests' do
    inherit! :search_paths

  end

  target 'DailyQuoteUITests' do
    inherit! :search_paths

  end

end
